import React, { Fragment } from "react";
import { Heading, Slide, Text } from "spectacle";

export default () => (
  <Slide bgColor="secondary">
    <Heading size={2} caps lineHeight={1} fit textColor="primary">
      Navigation
    </Heading>
  </Slide>
);
